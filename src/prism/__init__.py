# src/prism/__init__.py
"""Prism — a cross-sectional systematic trading engine.

v0.3.0 re-founds the project on the score -> residualize -> construct -> execute
spine (see SPEC.md); the per-symbol forecaster ensemble is demoted to one
plug-in signal node. The importable package is ``prism`` under src-layout
(``src/prism/``); the distribution name stays ``trading-ensemble`` until the
repository/distribution rename at publication (SPEC.md §12, HANDOFF.md).
"""

__version__ = "0.3.0"
